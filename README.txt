Mediawiki-Template für die Seite: http://wiki.freifunk-franken.de/
Ursprünglich erstellt von https://github.com/melle/ffp-webdesign/tree/master/mediawiki-skin

Änderungen:

# Änderungen am besten lokal auf dem eigenen Rechner vornehmen (z.B. im CSS).
# Die Änderungen aus dem Ordner mediawiki-skin" auf den Server in das Verzeichnis "/web/mediawiki/skins" kopieren.
# Falls die Datei".htaccess" geändert werden muss (ACHTUNG!) diese auf den Server in das Verzeichnis "/web" kopieren.
# Änderungen online testen.
# Wenn die Änderungen gut waren in git einchecken.
# Wenn die Änderungen schlecht waren Änderungen rückgängig machen und Originale schnell wieder auf den Server spielen.

Bitte auf keinen Fall Dateien mit Passwörtern (z.B. LocalSettings.php) einchecken!
